# Next Grok Build — arif-fazil.com agentic web

```yaml
context_manifest:
  class: operational_handoff
  author: grok-build
  binding: false
  created_at: 2026-09-13T06:45:00Z
  expires_at: 2026-09-20T00:00:00Z
  load_as: advisory
```

GitHub ops for this epoch are **closed as RECEIPT**. Do not re-ratify. Do not stamp kernel SEAL.

## Intent

Upgrade `https://arif-fazil.com` so humans, agents, and institutions can enter the same house GitHub now describes:

- Human: What / Why / Care in ≤3 clicks
- Agent: `llms.txt`, `rsl.xml`, `.well-known`, MCP doors
- Institution: GEOX as bounded earth organ; arifOS as judge; Arif as owner

This is **not** another A2A doctrine pass. Public A2A `POST /a2a` 405 stays HOLD.

## Always first

```
python3 /root/arif-fazil.com/scripts/web-zen/web_zen.py doctor
```

Doctor 2026-09-13T06:44Z: FAIL only on commodity oil/gas HTTP 500 (units later started). Missions, llms.txt, earth, doctrine, 000/999, mcp host were 200. SPA bundle `/assets/index-ai4WHo90.js`.

## One law

VERSION CONTROL FIRST. LIVE TREE SECOND.  
Never `rsync --delete` without `web_zen.py orphan`.  
Never hand-edit `/var/www/html`. Fix source in `/root/arif-fazil.com` then `make deploy`.  
Caddy reload = T3 888_HOLD unless Arif names it.

## Map

| Path | Role |
|---|---|
| `/root/arif-fazil.com` | canonical source (this repo) |
| `/root/arif-sites/sites/` | site packages (skill OP 1) |
| `/var/www/html/arif` | live SPA |
| `docs/AGENTIC_WEB_WORLD_MODEL.md` | dual plane: one human home, organ hosts as agent doors |
| `docs/CONSTITUTION.md` | site law |
| Skill | `FORGE-agentic-web-builder` |

Human world = `arif-fazil.com` only. Organ subdomains are MCP/agent doors, not rival brands.

## Suggested first forge (pick one, finish it)

1. Landing: GitHub About sentence + three-link offer (Arif / GEOX / arifOS) without federation dialect.
2. `/earth` and GEOX story: institution can see “computes, does not adjudicate.”
3. Agent surfaces: `llms.txt` / missions.json parity with the 2026-09-13 GitHub audience tables.
4. Doctor green: oil/gas commodity 200, not just systemd active.

Do **not**: Caddy matcher for AAA `/a2a`, key rotation, medical IDs, FRAME GitHub (archived), HERMES runtime commit.

## Holds carried

- Public A2A exact `/a2a` 405
- WELL degraded
- Merkle / signing lane
- Medical quarantine (Pilihan A)

## Success

A stranger can open arif-fazil.com and answer: who, what organ, what it will never do, where to enter — in ten seconds. Then one GEOX job on authorized data still pays the rent. That job is **not** this handoff.
